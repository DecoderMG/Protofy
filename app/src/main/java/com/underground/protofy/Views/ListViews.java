package com.underground.protofy.Views;

import android.content.Context;
import android.widget.ListView;

public class ListViews extends ListView {
    //ListView l = new ListView(null);
    public ListViews(Context lv) {
        super(lv);
    }
}
